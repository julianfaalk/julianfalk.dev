name: Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via webhook
        run: curl -s "https://www.julianfalk.dev/deploy.php?token=${{ secrets.DEPLOY_SECRET }}"
